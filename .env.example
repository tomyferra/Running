# Garmin Connect credentials
GARMIN_EMAIL=your@email.com
GARMIN_PASSWORD=yourpassword

# Database (default works for Docker; override for local dev)
DB_DIR=./data/garmin

# API settings (optional)
API_TITLE=Garmin Running API
API_VERSION=1.0.0
DEBUG=false
